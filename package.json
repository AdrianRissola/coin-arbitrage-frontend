{
  "name": "coin-arbitrage-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.3.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.1.3",
    "cross-env": "^7.0.3",
    "env-cmd": "^10.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-dotenv": "^0.1.3",
    "react-scripts": "^5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "dev": "cross-env NODE_ENV=development && react-scripts start",
    "start": "cross-env NODE_ENV=production && react-scripts start",
    "build": "cross-env NODE_ENV=production && react-dotenv && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "html-loader": "^1.3.2",
    "html-webpack-plugin": "^4.3.0",
    "react-error-overlay": "^6.0.9",
    "sass": "^1.54.6",
    "webpack-cli": "^4.10.0"
  },
  "react-dotenv": {
    "whitelist": [
      "URI"
    ]
  }
}
